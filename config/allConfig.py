from config.staticConfig.commands import commands
from config.staticConfig.hello import hello
from config.staticConfig.name import name
from config.staticConfig.tbr import tbr

opts = {
     "name": name,
    "cmds": commands,
    "hello": hello,
    "tbr": tbr,
    "activation": name
}
